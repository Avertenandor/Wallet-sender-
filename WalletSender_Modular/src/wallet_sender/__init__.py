"""WalletSender package init.

Keep empty/lightweight to avoid side effects during package import. Modules
should import what they need explicitly, e.g. `from .config import Config`.

Version of the WalletSender Modular application is exposed as `__version__`.
"""

__version__ = "2.1.0"
